# test-drive-reservation
## Installation
```bash
# リポジトリをクローン
git clone https://github.com/〇〇/〇〇.git
# 仮想環境を作成
python3.13.3 -m venv .venv
# 仮想環境を有効化
source .venv/bin/activate
# ライブラリをインストール
pip install -r requirements.txt
```

## Usage
```bash
# サーバを起動
python app.py
```
http://127.0.0.1:5000/ からWebサイトにアクセス<br>
CTRL+Cでサーバを終了

## ディレクトリ構成
```text
test_drive_reservation/
├── app.py                 # アプリケーションの起動と初期化（エントリーポイント）
├── extensions.py          # 拡張機能（DBなど）のインスタンス定義
├── config.py              # 環境変数・DB接続設定
│
├── models/                # 【Model層】 データベースのテーブル定義
│   ├── __init__.py        # パッケージ化のための空ファイル
│   ├── car.py             # 車種マスタテーブルの定義
│   └── reservation.py     # 予約トランザクションテーブルの定義
│
├── services/                  # 【Service層】 ビジネスロジックとDB操作
│   ├── __init__.py            # パッケージ化のための空ファイル
│   ├── reservation_service.py # 新規予約に関する処理（データ検証、DB保存など）
│   └── inquiry_service.py     # 予約照会・変更・キャンセルに関する処理（認証、DB更新など）
│
├── views/                 # 【Controller層】 ルーティングと画面遷移
│   ├── __init__.py        # パッケージ化のための空ファイル
│   ├── top.py             # Top画面のルーティング（Blueprint）
│   ├── reservation.py     # 新規予約フローのルーティング（Blueprint）
│   └── inquiry.py         # 予約照会・変更・キャンセルフローのルーティング（Blueprint）
│
└── templates/             # 【View層】HTMLテンプレート（Jinja2）
    ├── base.html          # 全画面共通のレイアウト（ヘッダー・フッターなど）
    ├── top.html           # Top画面
    ├── reservation/
    │   ├── select.html    # 車種・日時選択画面
    │   ├── customer.html  # お客様情報入力画面
    │   ├── confirm.html   # 入力情報確認画面
    │   └── complete.html  # 送信完了画面
    └── inquiry/
        ├── auth.html      # 本人確認画面
        ├── detail.html    # 予約照会画面
        ├── confirm.html   # 変更確認画面
        ├── cancel.html    # キャンセル確認画面
        └── complete.html  # 変更・キャンセル完了画面
```

## プロンプト

```text
試乗予約サイトを開発する．
以下の内容を読み，命令に従って生成をしてください．

---用件定義---
成果物：
	仮想の1店舗の試乗予約サイト．
背景：
	店舗スタッフは試乗予約業務を，電話や対面でのヒアリングやExcelでの予約管理で遂行している．
	そのため，無駄な工数や手作業によるミスが多く発生している．
目的：
	本システムによって，試乗予約業務の工数削減と正確性の向上を目指す．
システムの機能：
	次の４つの機能を持つ．
		1. 新規予約
		2. 予約照会
		3. 予約変更
		4. 予約キャンセル

---外部設計---
画面（ID）とレイアウト：
	・Top画面（0）：
			店名表示
			予約ボタン
			予約照会ボタン
	・車種・日時選択画面（1）
			車種選択フォーム
			日時選択フォーム
			戻るボタン
			次へボタン
	・お客様情報入力画面（2）
			姓入力フォーム
			名入力フォーム
			電話番号入力フォーム
			メールアドレス入力フォーム
			戻るボタン
			次へボタン
	・入力情報確認画面（3）
			車種表示
			日時表示
			姓表示
			名表示
			電話番号表示
			メールアドレス表示
			戻るボタン
			送信ボタン
	・送信完了画面（4）
			「送信完了しました」表示
			Topへボタン
	・本人確認画面（5）
			姓入力フォーム
			名入力フォーム
			電話番号入力フォーム
			戻るボタン
			次へボタン
	・予約照会画面（6）
			車種表示
			日時表示
			変更へボタン
			キャンセルへボタン
	・キャンセル確認画面（7）
	　　「本当に予約をキャンセルしますか？」表示
			はい．いいえのラジオボタン
			送信ボタン

機能ごとの画面遷移：
	新規予約: 0→1→2→3→4
	予約照会: 0→5→6
	予約変更: 0→5→6→1→3→4
	予約キャンセル: 0→5→6→7→4

---開発前提---
デプロイはしない．

---技術スタック---
・Flask
・Flask-SQLAlchemy
・Brueprint

---アーキテクチャ（設計思想）---
・MVC+Sモデル
・保守性を高くする（開発者間での役割分担，テスト，改修がしやすい）

---バイブコーディングの進め方---
1. 内部設計
2. プロダクションコードの実装
4. エラー処理をするコードの実装
5. テストコードの実装

---命令---

```
